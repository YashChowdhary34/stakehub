import React from "react";
import UserTransactionList from "../../components/UserTransactionList";

const page = () => {
  return (
    <main>
      <UserTransactionList />
    </main>
  );
};

export default page;
